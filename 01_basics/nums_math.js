const score = 400
console.log(score);
 
const balance = new Number(100); // this new keyword will make sure that ki "100" is defined as a number
console.log(balance);

console.log(balance.toString()); //this prototype will change the number to a string
console.log(typeof(balance.toString())); // checking the type of '100'
console.log(balance.toString().length); // and as number is now converted to a string we can check it's length
console.log(balance.toFixed(2)); // toFixed will give 2 digits after the decimal which we helps for the simplicity of the number
 
 
 
 