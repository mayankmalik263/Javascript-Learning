"use strict"; // treat all JS code as newer version

//alert("Hello") //: we are using nodejs, not browser.

console.log(1 
    +
     2); //code readability should be high

console.log("Mayank");

let name = "mayank"
let age = 17
let isLoggedIn = false
let state = null

//console.table([name, age, isLoggedIn, state]);


/*
number - range(2^53)
bigInt
string
boolean - true/false
null - standalone value (it is a representation of a empty value)
undefined - it is like not defining the value of anything
symbol - unique(mainly will be used we will work on react)


object
*/

console.log(typeof state); //this prints "object"! WHY? Interview question!!
console.log(typeof undefined); //undefined

